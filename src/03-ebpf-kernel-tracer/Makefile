all:
	@echo "Sottoprogetto 03: ebpf-kernel-tracer"
	@echo "Gli script bpftrace in scripts/ sono pronti per l'esecuzione diretta con sudo bpftrace"

run-sched:
	sudo bpftrace scripts/sched_latency.bt

clean:
	@echo "Pulizia completata."
